name: Backend CI
on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run Tests using Gradle image
        run: docker run --rm -v $PWD:/app -w /app gradle:8.7-jdk21 gradle test